package lpl.ast;

public abstract class PrimaryExp {
    
    @Override
    public String toString() {
        // no need to change this - add appropriate overrides in subclasses
        throw new Error("toString not overridden in class " + this.getClass().getName());
    }
}
